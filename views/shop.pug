extends layouts/my-layouts.pug

block styles
  link(rel="stylesheet" href="/css/main.css")

block content
  main.main 
    h1 My products:
      if prods.length > 0
        each product in prods
          li #{product.title}
      else 
        h1 No Products

<%- include('includes/head.ejs') %>
  </head>
  <body>
    <%- include('includes/navigation.ejs') %>
    <main class="main">
      <h1>My products</h1>
        <% if(prods.length > 0) {%>
          <% for (const product of prods) {%>
          <li><%= product.title %></li>
          <% } %>
        <% } else {%>
          <h1>No Products!</h1>
        <%  } %>
    </main>
  </body>
</html>
